# Commands

```
    $ sudo mysql 
    $ mysql -u root -p 
    Enter password:
    mysql>
```

```
    SHOW DATABASES;
    USE snippetbox;
    SHOW TABLES;
    SHOW TABLE STATUS;
    DESCRIBE table_name; or SHOW COLUMNS FROM table_name;
```